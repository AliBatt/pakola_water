name: repositories
description: Repository layer for Pakola Waters.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  core:
    path: ../core
  models:
    path: ../models
  services:
    path: ../services

dev_dependencies:
  flutter_lints: ^6.0.0

name: models
description: Shared domain models for Pakola Waters.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.5.4
  flutter_lints: ^6.0.0
  json_serializable: ^6.9.5

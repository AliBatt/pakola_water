name: shared_widgets
description: Reusable UI widgets shared across Pakola Waters apps.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  design_system:
    path: ../design_system
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

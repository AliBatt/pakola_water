name: services
description: Application services for Pakola Waters.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  core:
    path: ../core
  firebase:
    path: ../firebase
  models:
    path: ../models

dev_dependencies:
  flutter_lints: ^6.0.0
